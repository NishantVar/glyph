skill enforce_style()
    description: "Enforce coding style rules."
    require consistency
    avoid magic_numbers

const consistency = "Follow the project's established coding patterns and conventions."
const magic_numbers = "Using unexplained numeric literals in source code."
