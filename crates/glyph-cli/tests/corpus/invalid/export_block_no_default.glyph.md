skill caller()
    description: "Demonstrates an export-block parameter without a default."

    flow:
        "Run the helper."

export block helper(scope)
    description: "Helper missing a default for scope."

    flow:
        "Inspect a directory."
