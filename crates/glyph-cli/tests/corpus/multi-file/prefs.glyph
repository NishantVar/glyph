// Team-wide coding preferences.

export const preserve_existing_patterns = "prefer the repository's existing patterns, helper APIs, naming, and file organization before introducing a new abstraction or style"

export const safety_first = "never execute destructive operations without explicit confirmation"

export const minimal_changes = "make the smallest change that solves the problem"
