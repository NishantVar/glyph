---
name: predicate_const_single_arm
description: 'Demonstrate single-arm pure-predicate const form.'
---

## Steps

1. Decide whether the requested change requires regenerating multi-line prose that repair or prose-reshape originally authored, beyond a localised wording or value swap applies and, if so:
   a. Stop and recommend running `/glyph:compile` instead — incremental edit cannot regenerate prose.

