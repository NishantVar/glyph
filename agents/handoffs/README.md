# agents/handoffs

Durable home for approved role-to-role and Maya/Ari handoff artifacts. A handoff
captured here should name the role(s), owned work, adjacent boundaries, and the
concrete next step so the receiving owner can act without reconstructing context
from chat. Owned by the Integration Engineer (`runtime-integration-engineer-agent`)
as `agents/**` scaffold mechanics.
